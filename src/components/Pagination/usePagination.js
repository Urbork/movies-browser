export const usePagination = (pages, setPages) => {
  // const first = () => {
  //   if (pages.currentPage <= pages.firstPage) return;
  //   setPages(prevState => ({ ...prevState, currentPage: pages.firstPage }))
  // };

  // const previous = () => {
  //   if (pages.currentPage <= pages.firstPage) return;
  //   setPages(prevState => ({ ...prevState, currentPage: prevState.currentPage-- }))
  // };

  // const next = () => {
  //   if (pages.currentPage >= pages.lastPage) return;
  //   setPages(prevState => ({ ...prevState, currentPage: prevState.currentPage++ }))
  // };

  // const last = () => {
  //   if (pages.currentPage >= pages.lastPage) return;
  //   setPages(prevState => ({ ...prevState, currentPage: prevState.lastPage }))
  // };

  // return {
  //   first,
  //   previous,
  //   next,
  //   last,
  // };
};